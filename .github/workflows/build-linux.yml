name: Build Linux

on:
  push:
    branches: [main, "devin/**"]
    tags: ["v*"]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore
        run: dotnet restore PcLun.sln

      - name: Build (Release)
        run: dotnet build PcLun.sln -c Release --no-restore

      - name: Run tests
        run: dotnet test PcLun.sln -c Release --no-build --verbosity minimal

      - name: Publish self-contained Linux x64
        run: >
          dotnet publish src/Launcher/Launcher.csproj
          -c Release
          -r linux-x64
          --self-contained true
          -p:PublishSingleFile=true
          -p:IncludeNativeLibrariesForSelfExtract=true
          -p:EnableCompressionInSingleFile=true
          -o publish/linux-x64

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: PcLun-linux-x64
          path: publish/linux-x64/PcLun
          if-no-files-found: error

      - name: Rename for release
        if: startsWith(github.ref, 'refs/tags/v')
        run: cp publish/linux-x64/PcLun publish/linux-x64/PcLun-linux-x64

      - name: Create Release on tag
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: publish/linux-x64/PcLun-linux-x64
          generate_release_notes: true
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
